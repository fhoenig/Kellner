// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("event", "for event support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("event", "enable event support", "no");

if (PHP_EVENT != "no") {
	EXTENSION("event", "event.c");
}

